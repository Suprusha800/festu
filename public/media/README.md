# Campus Map Image

The project includes a default map image:

`public/media/campus-map.svg`

The interactive blocks are configured in:

`src/components/CampusMap.tsx`

Edit the `mapAreas` array to change each polygon point.
